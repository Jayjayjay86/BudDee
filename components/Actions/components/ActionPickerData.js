import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ActionPickerData = () => {
  return (
    <View>
      <Text>ActionPickerData</Text>
    </View>
  );
};

export default ActionPickerData;

const styles = StyleSheet.create({});
